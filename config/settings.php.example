<?php
$settings = [
    'site_name' => 'DataDock',
    'admin_contact_email' => '',           // Shown in footer; leave empty to hide
    'registration_enabled' => true,
    'enforce_unique_email' => true,        // If false, allow duplicate emails (relaxed)
    'max_file_size' => 5242880,             // 5 MB
    'default_file_expiry' => 'never',      // Default for upload form: 1_minute, 30_minutes, 1_hour, 6_hours, 1_day, 1_week, 1_month, 1_year, never
    'thumbnails_enabled' => true,          // Generate thumbnails for image uploads
    'session_timeout_minutes' => 60,       // Session lifetime in minutes; 0 = until browser close
    'install_warning_enabled' => true,     // Show admin warning when install.php exists

    'maintenance_mode' => false,           // Block non-admins when enabled
    'debug_mode' => false,                 // Show PHP errors; disable in production
    'log_path' => '',                      // e.g. logs/app.log; empty = no file logging
    'log_level' => 'warning',              // debug, info, warning, error

    'logo_url' => '',                      // Custom logo; empty = use site name text
    'favicon_url' => '',                   // Custom favicon URL
    'welcome_message' => '',               // Editable message on homepage
    'theme' => 'light',                    // light or dark; users can toggle
    'file_icons' => [],                   // Override icons: {'pdf'=>'📄','doc'=>'📝'}

    'tos_enabled' => false,                // Require ToS acceptance before upload
    'tos_text' => '',                     // ToS / Acceptable Use policy text

    'brute_force' => [
        'enabled' => true,
        'max_attempts' => 5,
        'lockout_minutes' => 15,
        'lockout_window' => 10
    ],

    'guest_uploads' => [
        'enabled' => false,
        'max_files' => 0,
        'max_storage' => 0
    ],

    'user_limits' => [
        'max_files_enabled' => false,
        'max_files' => 100,
        'max_storage_enabled' => false,
        'max_storage' => 104857600 // 100 MB
    ]
];
?>
